# Local generated HTML from generate_gantt.py.
# The published copy lives on the gh-pages branch (and ~/infra-testing-roadmap) as index.html.
Product Testing Roadmap.html
Product_Testing_Roadmap.html

# Publish artifacts — these live on the gh-pages branch, not on main.
index.html
plan.json

# Result ingestion data for import_results.py — screenshots + per-experiment .xlsx + processed/
results/

# macOS / Excel cruft
.DS_Store
~$*.xlsx

# Python
.venv/
__pycache__/
*.pyc
